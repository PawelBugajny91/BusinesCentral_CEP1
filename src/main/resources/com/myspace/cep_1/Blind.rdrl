package com.myspace.cep_1;

import java.lang.Number;

rule "Blind"
	dialect "mvel"
	activation-group "cep"
	salience 5
	when
		ares : AresEvent( ) over window:time (2s)
	then
		Alert fact1 = new Alert();
		fact1.setDuration( ares.duration );
		fact1.setTime( ares.time );
		insert( fact1 );
end
