package com.myspace.cep_1;

import java.lang.Number;

rule "Blind"
	dialect "mvel"
	when
		ares : AresEvent( ) over window:time (2s)
	then
		Alert fact0 = new Alert();
		fact0.setTime( ares.time );
		fact0.setDuration( ares.duration );
		insert( fact0 );
end
